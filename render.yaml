services:
  - type: web
    name: true-forge
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: DATABASE_URL
        value: sqlite:////tmp/trueforge.db
      - key: DATA_DIR
        value: /tmp/data
